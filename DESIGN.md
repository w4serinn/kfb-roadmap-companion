# kfb-roadmap-companion 設計書(ドラフト v0.1)

## 1. プロジェクトの目的

Kawaii Future Bass作曲学習ロードマップ(1年計画)を、実際に最後まで走り切るための「支援サイト」を作る。

**このアプリが解決する課題:**
- 週7時間ペースの継続を、意思力だけに頼らせない
- 「完コピ・完成度」への過剰なこだわりが挫折の主因になっているため、各タスクに明示的な合格ラインを見せる
- 4ヶ月目に予定されている耳コピが最大の不安ポイントなので、専用の練習ツールで「才能」ではなく「手順」に変換する

**このアプリが解決しない課題(非ゴール):**
- 実際の作曲・DAW操作そのものの代替(FL Studio / Vitalの役割はそのまま)
- 音源ファイルの管理・DAWプロジェクトのバックアップ
- SNS共有や他者との比較機能(挫折対策の1番目「比較対象は先週の自分だけ」という方針と矛盾するため、意図的に作らない)
- AIによるメロディー・楽曲の自動生成(レイヤー4はあくまで「本人が頭の中で思いついたメロディーの採譜支援」であり、AIが代わりに作曲する機能ではない。この境界線は明確に維持する)
  - レイヤー5(コード候補提示)は例外的に許容する。これは音楽理論に基づく
    決定論的な照合ロジック(ダイアトニックコードとの構成音一致)であり、
    創作的な自動生成ではない。かつ候補は必ず複数提示し、単一の正解を
    返す形にはしない

## 2. 全体構成:3つのレイヤー

### レイヤー1:進捗レイヤー(既存チェックリストの発展形)
- 週タスク・スモールステップのチェック(1〜4ヶ月目は日次ステップまで分解、5ヶ月目以降は週・曲単位)
- 各タスクに「合格ライン」を常時表示するUI要素(例:耳コピ週なら「ベース音1音取れれば合格」)
- 最終操作日を記録し、7日以上間隔が空いたら「今日はページを開いて既存プロジェクトを再生するだけでOK」という復帰用の軽量表示に切り替える
- 制作系タスクには「バージョン1」「バージョン2」と番号を刻める仕組みを用意し、無限に完成度を追い求めず「一旦完成」で次に進む行動を後押しする

### レイヤー2:耳コピ支援レイヤー(4ヶ月目向け専用ツール)
- キー選択(12キー)→選んだキーのダイアトニックコード(I〜vii°)を自動表示
- 8小節分、コードを1つずつ選んで並べる入力UI(理論知識が薄くても表から選ぶだけで進行が組める)
- **コード試聴機能**:Web Audio APIでダイアトニックコードをその場で生成・再生。Maj7/m7の響きの違いも聴き比べ可能
- **リズムパターン試聴機能**:基本形(四つ打ち、Future Bassのハイハット刻み等)をプリセットで再生
- 最後にChordify / Hooktheoryへの橋渡しリンクで答え合わせ
- 答えが合っていなくても「ベース音だけ合っていればOK」の基準をその場に表示

### レイヤー3:記録レイヤー(挫折対策の仕組み化)
- 週末に「今週は制作/理論/耳コピ、どのくらい触れた?」を数値ではなく感覚(3段階程度)で聞く軽いチェックイン
- タスクごとに一言メモ欄(詰まった点・気づき。日記ほど重くしない)

### レイヤー4:メロディー起こし支援(鼻歌→音名変換)
「頭の中にあるメロディーを自分で書き起こすのが不安」という声から生まれたレイヤー。
AIがメロディーを代わりに作る機能ではなく、**すでに頭の中にあるメロディーを、
自分で採譜するのが難しいから手伝う**採譜支援ツール。作曲の判断そのものは奪わない、
という非ゴールの境界線を維持する。

- マイクで鼻歌を録音
- ブラウザ内で自己相関法(autocorrelation)による音高解析(サーバー不要、Web
  Audio APIのみで実現可能)
- 検出した周波数を最も近い音階に丸めて、音名の並び(例:ド→ミ→ファ→ソ)として表示
- 音の切れ目(ブレス・音高変化)でフレーズを分割
- 検出結果を再生し、意図したメロディーと聴き比べられるようにする

**技術的な前提・制約**
- 単音(鼻歌)前提。和音の検出は対象外
- ピッチの検出精度は比較的高いが、リズム(何拍目に音が来たか)の精度は
  ブラウザ内処理だけでは粗くなりがちなため、最初のバージョンは
  「ピッチは目安以上、リズムはあくまで目安」という期待値に留める
- 耳コピ支援レイヤー(レイヤー2)とWeb Audio APIの技術基盤を共有できる

### レイヤー5:コード候補提示(メロディー→合うコードの提案)
メロディーの音から、理論的に合う可能性のあるコードを提示するレイヤー。判断は
機械学習的な「創作」ではなく、音楽理論に基づく決定論的な照合ロジックで行う
(キーのダイアトニックコードと、メロディー各音の構成音一致を調べるだけ)。

- キーを指定(またはレイヤー4で捕まえたメロディーから推定)
- そのキーのダイアトニックコード(I〜vii°)を一覧化
- メロディーの各音が、どのコードの構成音に含まれるかを機械的に照合し、
  候補として提示する
- フレーズ全体を通した「候補になりやすい進行パターン」も複数提示する
- 候補を選ぶと、メロディーと合わせて再生し聴き比べられる

**設計上の境界線**
候補は必ず複数提示し、1つに決めつけない。「これが正解」と単一の答えを
返す機能にはしない。レイヤー4で捕まえたメロディーに対してこの機能を繋げると、
「鼻歌→音名化→コード候補提示→聴き比べ→FL Studioで本格制作」という
一連の流れが作れる。

## 3. 技術方針

- **形態**:単一の静的サイト。`theme-park-loop`と同じくGitHub Pagesでの公開を想定
- **データ保存**:ブラウザの`localStorage`(このプロジェクトは独立した静的サイトなので、Claude.aiアーティファクトのstorage APIとは無関係。完全に自分の管理下に置ける)
- **音声**:音源ファイルなしでWeb Audio APIによるシンプルなシンセ音生成(コード試聴・リズム試聴の両方をこれで実現し、依存を増やさない)
- **構成言語**:HTML/CSS/JS(静的サイトなのでフレームワーク不要。`theme-park-loop`のスタックに合わせても良い)

## 4. 優先順位(フェーズ分け)

| フェーズ | 内容 | 目安タイミング |
|---|---|---|
| フェーズ1 | レイヤー1(進捗+挫折対策の仕組み)を固めて実運用開始 | 今すぐ〜1ヶ月目 |
| フェーズ2 | レイヤー3(週末チェックイン・メモ欄)を軽く追加 | 2〜3ヶ月目 |
| フェーズ3 | レイヤー2(耳コピ支援ツール)を本格実装 | 4ヶ月目に入る直前(3ヶ月目後半) |
| フェーズ4 | レイヤー4(メロディー起こし支援)を実装 | 2ヶ月目「8小節ループ組み立て」でメロディーに触れ始める頃を目安に、フェーズ3と並行または直後 |
| フェーズ5 | レイヤー5(コード候補提示)を実装 | フェーズ4完了後。レイヤー4で捕まえたメロディーと連携させる想定 |

1年計画のアプリなので、アプリ自体も焦って全部作らず、ロードマップの進行に合わせて育てる想定。

## 5. リポジトリ構成(案)

```
kfb-roadmap-companion/
├── CLAUDE.md          # loop engineeringのmaker/checker運用ルール
├── ROADMAP.md         # このアプリ自体の開発ロードマップ(タスク粒度に注意、アンカーバイアス回避)
├── DESIGN.md          # 本書
├── index.html         # レイヤー1(進捗トラッカー)
├── ear-training/       # レイヤー2(耳コピ支援ツール、フェーズ3で追加)
│   └── index.html
├── melody-capture/     # レイヤー4(メロディー起こし支援、フェーズ4で追加)
│   └── index.html
├── chord-suggest/      # レイヤー5(コード候補提示、フェーズ5で追加)
│   └── index.html
└── assets/
    ├── audio-engine.js  # コード/リズム生成ロジック(共有モジュール)
    ├── pitch-detect.js  # 自己相関法による音高解析ロジック(共有モジュール)
    └── harmony-match.js # メロディー音とダイアトニックコードの照合ロジック(共有モジュール)
```

## 6. Claude Codeループへの引き渡しにあたっての注意

- `theme-park-loop`と同様、design doc(本書)→`CLAUDE.md`→`ROADMAP.md`の順で用意し、maker/checker分離で反復する運用を想定
- レイヤー2(耳コピ支援)は音のプレビューという「聴覚での検証」が必要になるため、checker側が客観的に検証しづらいタスクになりやすい。ROADMAP.md作成時は、この部分のタスク記述を「動作確認手順が明確な単位」まで分解しておくと、loopが空回りしにくい
- レイヤー1の「7日空いたら復帰表示切り替え」のような日付ベースのロジックは、テスト時に日付をモックできる設計にしておくとcheckerが検証しやすい

---

*本書はv0.1のドラフト。Claude Codeでのループ開始後、実装の都合で構成が変わることは前提とする。*
