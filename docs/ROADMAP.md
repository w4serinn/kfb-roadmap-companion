# ROADMAP

## プロジェクトの目的(確定)

Kawaii Future Bass作曲学習ロードマップ(1年計画)を実際に走り切るための支援サイト。
音楽制作アプリの代替ではなく、継続と理解の橋渡し役に徹する。

## 非ゴール(確定・変更しない)

- 実際の作曲・DAW操作そのものの代替(FL Studio / Vitalの役割はそのまま)
- 音源ファイルの管理・DAWプロジェクトのバックアップ
- SNS共有や他者との比較機能(「比較対象は先週の自分だけ」という方針と矛盾するため)
- AIによるメロディー・楽曲の自動生成(レイヤー4は本人が思いついたメロディーの
  採譜支援であり、AIが代わりに作曲する機能ではない)
  - レイヤー5(コード候補提示)は例外的に許容する。ダイアトニックコードとの
    構成音一致という決定論的な照合ロジックであり、創作的な自動生成ではない。
    候補は必ず複数提示し、単一の正解を返す形にはしない

## フェーズ(確定・優先順位)

1. **フェーズ1(現在)**: 進捗レイヤー — 週/日次チェックリスト、挫折対策の仕組み
   (復帰表示・合格ライン表示・バージョン番号)
2. **フェーズ2**: 記録レイヤー — 週末チェックイン、タスクごとの一言メモ欄
3. **フェーズ3**: 耳コピ支援レイヤー — キー選択・ダイアトニックコード表示・
   コード/リズムのプレビュー再生・答え合わせへの橋渡し
4. **フェーズ4**: メロディー起こし支援レイヤー — 鼻歌の録音・自己相関法による
   音高解析・音名への変換・再生確認(単音前提、リズムは目安程度の精度)。
   フェーズ3とWeb Audio APIの技術基盤を共有するため、並行または直後の着手も可
5. **フェーズ5**: コード候補提示レイヤー — メロディー音とダイアトニックコードの
   照合による候補提示(必ず複数候補、単一の正解にしない)。フェーズ4完了後、
   レイヤー4で捕まえたメロディーと連携させる想定

フェーズを超えた先取り実装はしない(evolve/SKILL.md参照)。フェーズが進むタイミングは
人間が`docs/ROADMAP.md`のこのセクションを書き換えることで示す。

## バグ修正(最優先 — 通常機能より先に上から順に着手する)

(現在なし)

## 新機能追加提案(承認待ち — 承認されたら下の「機能一覧」に移動する)

現在のフェーズ外の新機能提案、または既存機能への大きな追加提案はここに書くこと。
実装には進まない。

(現在なし)

---

## 機能一覧と進行状況

各機能はS/Mサブタスクに分割し、数サイクルかけて完成させる。

### 0. プロジェクト基盤(セットアップ) [status: 完了]

### 1. 進捗トラッカー(既存チェックリストの移植) [status: 進行中]

- [ ] 全体進捗を可視化する表示(シーケンサー風など、デザインは自由)を実装する

### 2. 挫折対策の仕組み [status: 進行中]

- [ ] 最終操作日から7日以上経過している場合、通常の週タスク表示の代わりに
      「今日はページを開いて既存プロジェクトを再生するだけでOK」という
      復帰用の軽量表示に切り替えるロジックを実装する(現在時刻を引数で
      渡せる形で実装し、テスト可能にする)
- [ ] 各週タスクに「合格ライン」を表示するUI要素を追加する
      (例: 耳コピ週なら「ベース音1音取れれば合格」。文言は`docs/MESSAGES.md`の
      承認済みリストから使う)
- [ ] 制作系タスクに「バージョン1」「バージョン2」と番号を刻める入力欄を実装する

---

## 今後のタスク候補(フェーズ2以降の参考。着手はフェーズが進んでから)

- 週末チェックイン(3段階の感覚入力)の実装
- タスクごとの一言メモ欄の実装
- キー選択→ダイアトニックコード自動表示の実装
- Web Audio APIによるコード試聴機能の実装
- Web Audio APIによるリズムパターン試聴機能の実装
- Chordify/Hooktheoryへの橋渡しリンクの実装
- 自己相関法による音高解析ロジック(`assets/pitch-detect.js`)の実装
- マイク録音→音名変換→再生確認のUI実装(フェーズ4)
- メロディー音とダイアトニックコードの照合ロジック(`assets/harmony-match.js`)の実装
- コード候補提示UI(複数候補表示・メロディーと合わせた再生確認)の実装(フェーズ5)
